using System.Collections;
using System.Collections.Generic;
using UnityEngine;


// A line segment on the map

[CreateAssetMenu(fileName = "TornadoWeather", menuName = "Weather/Tornado")]
public class TornadoWeather : ScriptableObject
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
